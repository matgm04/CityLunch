framework:
    secret: '%env(APP_SECRET)%'
    session:
        handler_id: null
        cookie_secure: auto
        cookie_samesite: lax
    csrf_protection: true
    http_method_override: true
    assets: ~
    php_errors:
        log: true
    form:
        csrf_protection:
            enabled: true